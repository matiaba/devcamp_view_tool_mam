RSpec.describe DevcampViewToolMam do
  it "has a version number" do
    expect(DevcampViewToolMam::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
